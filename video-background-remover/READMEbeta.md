# 1. Create the virtual environment named 'racss'
python3 -m venv GhostStream

# 2. Activate the environment
source GhostStream/bin/activate

# 3. Install the required libraries directly
pip install opencv-python PyQt5 numpy

# to run (depends on where u are):
python src/main.py or python main.py